{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome with Development Server",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start Development Server"
    }
  ]
}
